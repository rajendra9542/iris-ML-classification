# iris-ML-classification
Machine Learning project using the Iris dataset to classify flower species based on sepal and petal measurements. Implemented multiple algorithms such as KNN, Logistic Regression, Decision Tree, Random Forest, and SVM. Includes data preprocessing, model training, evaluation, prediction, and algorithm performance comparison using Python &amp; scikit
